To import all modules:
  $ npm install

To view gulp targets:
  $ gulp -T

To compile code and run test server:
  $ gulp serve

View latest version here:
  http://mrdunk.github.io/game_of_tides_4/frontend2/dist/index.html
